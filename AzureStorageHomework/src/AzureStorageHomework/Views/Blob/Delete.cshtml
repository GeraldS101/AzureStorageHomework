<!DOCTYPE html>
<html>
<head>
    <title>Delete Blob</title>
</head>
<body>
    <div id="page-wrapper">
        <div class="row">
            &nbsp;
        </div>
        <div class="row">
            <div class="col-lg-6">
                <div class="panel panel-danger">
                    <div class="panel-heading">
                        <h2>Are you sure to delete this blob?</h2>
                    </div>
                    <div class="panel-body">
                        <div class="row">
                            <div class="col-lg-12">
                                @using (Html.BeginForm("DeleteBlob", "Blob", routeValues: new { name = ViewBag.name }))
                                {
                                    @Html.AntiForgeryToken()
                                    <span>File Name: <strong>@ViewBag.name</strong></span><br /><br />

                                    <input type="submit" class="btn btn-primary" value="Delete" />
                                    @Html.ActionLink("Back to List", "Index", null, htmlAttributes: new { @class = "btn btn-default" })
                                }
                            </div>
                            <!-- /.col-lg-12 (nested) -->
                        </div>
                        <!-- /.row (nested) -->
                    </div>
                    <!-- /.panel-body -->
                </div>
                <!-- /.panel -->
            </div>
            <!-- /.col-lg-6 -->
            <div class="col-lg-6">
                &nbsp;
            </div>
        </div>
    </div>
</body>
</html>